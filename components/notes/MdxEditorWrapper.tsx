// Legacy dynamic MDX editor wrapper — removed.
// The project now uses `components/config/ForwardRefEditor` and
// `components/config/InitializeMDXEditor` for a single, pre-initialized
// MDX editor integration. Keeping this file as a no-op stub for now to
// avoid accidental imports; you can delete it entirely when ready.

export default function MdxEditorWrapper() {
  return null;
}
